# Input

Place `GSE226365_BioLRAF_scores.csv` in this directory.

Do not commit large Seurat objects or the full gene-level gficf matrix here.
The public example input should contain cell-level Os, Ch, Ad, and MSC scores
plus `cell_id`, `celltype`, and `dataset`.

