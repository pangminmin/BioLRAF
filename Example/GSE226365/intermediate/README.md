# Intermediate files

`GSE226365_gficf_matrix.csv` is generated locally by `BioLRAF_analysis.R`.
Because the complete cell-by-gene matrix may be large, it does not need to be
committed to GitHub. Its generation command is documented in `UPLOAD_STEPS.md`.

